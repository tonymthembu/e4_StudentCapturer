@model IEnumerable<string>  

@if (Model.Any())
{
    <div class="alert alert-danger">
        <ul>
            @foreach (var error in Model)
            {
                <li>@error</li>
            }
        </ul>
    </div>
}